name: "PHP"

on:
  pull_request:

  push:
    branches: [ "*" ]

  schedule:
    - cron: "0 8 * * 1"

jobs:
  build:
    name: "Build"
    uses: dockette/.github/.github/workflows/php.yml@v1
    with:
      name: "Build"
      php: "8.4"
      run: "composer validate"
